VITE_API_BASE=http://localhost:5101
# Tenant id is no longer set via env. Pick a real tenant from the dev login picker
# at http://localhost:5173/login (available only when running on localhost in DEV).
# VITE_TENANT_ROLE defaults to "tenant" when omitted; override only to exercise
# alternate role-based policies.
VITE_TENANT_ROLE=tenant
